import React from "react";
import { HeaderContainer, Logo } from "./styles/header.styles.js";

const Header = () => {
  return (
    <HeaderContainer>
      <Logo />
    </HeaderContainer>
  );
};

export default Header;
